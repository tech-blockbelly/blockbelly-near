import React from 'react';

const Signup = () => {
    return (
        <div className="signup-container">
            <h4 className="signup-title">Don't have an account? </h4>
            <a href="/signup" className="signup-btn btn">
                Signup
            </a>
        </div>
    );
};

export default Signup;
